import UIKit

let maxNumber = 10
var currentNum = 20

//maxNumber = 5  //常量不能直接赋值。报错

currentNum = 1 //变量可以赋值。

// 一行定义多个常量 或者变量
let x = 0.0, y = 0.0, z = "123"

print("x = \(x), y =\(y), z = \(z)")

let ab = "abc"
